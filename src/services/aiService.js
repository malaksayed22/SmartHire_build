// src/services/aiService.js
// DEPRECATED — All AI features now route through the main backend (port 3000).
// Import from src/services/api.js:
//   - scoreResumeByJob(jobId, file)
//   - chatByJob(jobId, question)
//   - rankCandidatesByPost(postId)
